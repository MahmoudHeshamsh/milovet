import 'package:flutter/material.dart';

class HomeTabOwner extends StatelessWidget {
  const HomeTabOwner({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('HomeTabOwner',style: TextStyle(color: Colors.amber),),),
    );
  }
}