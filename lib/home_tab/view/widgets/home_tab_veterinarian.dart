import 'package:flutter/material.dart';

class HomeTabVeterinarian extends StatelessWidget {
  const HomeTabVeterinarian({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('HomeTabVeterinarian',style: TextStyle(color: Colors.amber),),),
    );
  }
}